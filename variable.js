//JAVASCRIPT DAY-01
//VARIABLES.
let name='foo'// let is the keyword used to define the variable and it is irrespective of the data type.
let last_name='bar'
const name=' foos'//const is the keyword which fix the value of the variable and it can't be altered.

let fullname=name+last_name//here we are concatenating the two variable which holds strings.
console.log(fullname)//printing out the variable

console.log(name+ " "+ last_name)

let score=100// no. are initialized without any double or single quote.
let name1='phil'
console.log('score of '+name1+' '+' is'+' '+score)